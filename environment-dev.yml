name: crt1d-dev
channels:
  - conda-forge
dependencies:
  - python=3.10
  #
  # used by crt1d (prefer Conda versions though)
  - matplotlib
  - numpy
  - pandas
  - pyyaml
  - scipy
  - xarray
  #
  # for docs work
  - jupytext
  - jupyterlab>=3
  - sphinx=6
  - sphinx-autobuild
  #
  - pip  # otherwise conda nags
  - pip:
    # the package, including extras
    - '-e .[docs]'
  #
  # dev tools
  - rope
